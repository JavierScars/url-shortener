generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model ShortenedUrl {
  id        Int      @id @default(autoincrement())
  url       String
  hash      String   @unique
  createdAt DateTime @default(now())
}
